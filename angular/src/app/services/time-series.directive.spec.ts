import { TimeSeriesDirective } from './time-series.directive';

describe('TimeSeriesDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeSeriesDirective();
    expect(directive).toBeTruthy();
  });
});
